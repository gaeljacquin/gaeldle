{
  "name": "@workspace/api",
  "private": true,
  "scripts": {
    "dev": "INFISICAL_DISABLE_KEYRING=true ../../scripts/infisical-dev.sh run --path=/api --env=dev -- go run main.go || go run main.go",
    "build": "go build -o api main.go",
    "lint": "go vet ./...",
    "format": "gofmt -l .",
    "format:write": "gofmt -w ."
  }
}
